#!/usr/bin/env sh
# DISARMED 2026-09-01 (operator ruling D2, see .truth/FROZEN.md):
# this hook ran `truth invalidate-scan` + `auto-reaffirm.sh` on every
# commit -- the false-alarm engine (555/630 stalings false, >3:1).
# The ledger is frozen; nothing may write to it on a commit path.
exit 0
