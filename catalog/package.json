{
  "name": "catalog",
  "version": "0.1.0",
  "description": "Material catalog browser — YAML → JSON → Vite",
  "private": true,
  "scripts": {
    "dev": "node dev.js",
    "build": "node data/materials/build.js",
    "test": "node --test data/materials/tests/validate.test.js",
    "validate": "node data/materials/build.js && node --test data/materials/tests/validate.test.js",
    "preview": "npx vite preview --config vite.config.mjs"
  },
  "dependencies": {
    "js-yaml": "^5.1.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "vite": "^8.1.0"
  }
}
