[project]
name = "kitchen-app"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.13.4",
    "pytest>=9.0.3",
    "reflex>=0.9.2",
    "sqlmodel>=0.0.38",
]

[tool.pytest.ini_options]
pythonpath = [
  "."
]
