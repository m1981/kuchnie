[project]
name = "kitchen-generator"
version = "0.1.0"
description = "Config-driven kitchen cabinet generator for Blender"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]
