# Truth-Ledger Efficacy Trial — Log

> Reader: anyone assessing whether the truth ledger *helps* (not merely runs) on a real repo | Enables: comparing ledger behavior against the hand-maintained freeze docs over time | Update-trigger: any trial event — staleness firing, verdict filed, hand-audit run, upstream defect found

**Trial question:** does the ledger keep migration-state facts honest better
than hand-maintained status docs (`docs/freeze/*`, the deleted `RESUME.md`)?

**Started:** 2026-07-08 · **Repo:** kuchnie monorepo · **Regime:** solo dev + agent sessions, trunk
**Roles:** developer + monitor = Claude agent sessions; human judgments (retraction, divergence triage, monthly audit) = Michal

---

## Protocol

1. **Day-0 baseline** — 10 claims (tr-ec698a92 … tr-8ed0a7ff) filed from
   *re-verified current reality*, not from the freeze docs. Statuses start
   `unverified`; independent verification via `truth dispatch` is the next step.
2. **During development** — agents check `truth list --live` before trusting,
   file claims after verifying (AGENTS.md snippet), and let hooks demote.
3. **Metrics collected here:**
   - staleness events: tripwire fired vs. did the fact actually change
     (tripwire *precision*);
   - facts that changed with NO tripwire firing (tripwire *recall* — found
     only by hand-audit);
   - claims filed per agent session (discovery compliance);
   - queue latency: days from demotion to human resolution.
4. **Monthly hand-audit** — sample claims, compare to ground truth by hand,
   count false-VERIFIED. Day-0 baseline for comparison: the freeze docs'
   error rate (see "Prior decay evidence" below).

## Prior decay evidence (why the trial exists)

`MIGRATION-STATUS-2026-07.md` — immutable snapshot, ~2 weeks old — already
contains **2 rows contradicted by code** at trial start:

| Doc said | Reality at day 0 |
|---|---|
| ADR-010 deletion trio "still present, deletion pending" | all three files deleted; machining.py imports `kuchnie_core` |
| ADR-009 pyproject "drifted (`kitchen-generator`, deps=[])" | fixed: `name = "home-builder-adapter"`, real deps |

That is the baseline error rate hand-maintained docs produce in ~2 weeks.
The ledger must beat it to earn its rent.

## Wiring decisions (2026-07-08)

- **husky owns `core.hooksPath`** (`"prepare": "husky"` re-seizes it on every
  `pnpm install`); truth hooks are chained from `.husky/pre-commit` /
  `post-commit` / `post-merge`, delegating to the same scripts as `.githooks/`.
  `.githooks/` kept as copier-managed reference — do not wire it directly.
- Gate verified **live** (not by reading): malformed staged ledger line →
  commit blocked (INV-B) through the actual `$hooksPath` chain.

## Upstream findings (defects in truth-ledger itself, found by this trial)

| ID | Severity | Finding | Status |
|---|---|---|---|
| TL-1 | High | `doctor` reported hooks OK while hooks were dead: it checks hook *file contents*, not the effective `core.hooksPath` chain (husky had seized it). Doctor's one job is "your repo is wired" and it failed open on day 0. Fixed: doctor resolves `core.hooksPath`, follows husky-style `_` delegation one level up, accepts the scan in post-merge or post-commit, and names the file that satisfied the check. Regression-verified live: severing the husky truth line flips doctor to FAIL with an exact diagnosis. Gated by two new canary checks. | **fixed** in repo + template |
| TL-2 | Low | `check-truth.sh` on macOS/BSD: `head -n 0` is illegal when the committed ledger is empty; outcome accidentally correct (cmp empty-vs-empty). Guard `OLD_N -gt 0`. First macOS datapoint (paper claims Linux-only reproduction). | reported here; not yet fixed upstream |
| TL-3 | High | **Dispatch prompt integrity is transport-dependent.** An output-compression proxy (sqz) in the agent harness silently dropped rule 3 (VERDICT RULES — including "never soften a diverge into an agree") from `truth dispatch` output in **all 10** verifier sessions. G11 scripts what the verifier receives, but nothing guarantees it *arrives* intact. Fixed: the dispatch envelope now self-describes its integrity — a header states the exact numbered-rule count and announces a terminator line carrying the sha256 of the prompt file (comparable to `shasum -a 256 prompts/truth-verifier.md`); the claim record sits inside the envelope so record truncation also breaks the terminator. A reader of a lossily-compressed copy can now detect the loss and fall back to reading the prompt from disk. Gated by canary FAULT P + 2 unit tests. | **fixed** in repo + template |
| TL-4 | Medium | `truth verdict <id> --recheck` is not a dry-run: it silently **files an auto-`agree` verdict** ("recheck: output hash matches recorded evidence") before the verifier's interpretation step. Every protocol-obedient verifier therefore double-files; a verifier diverging at step 2 would leave contradictory verdicts; a verifier that stopped after step 1 would appear to have completed verification. Nothing in the prompt documents the write. Fixed: a matching recheck reports and files nothing (with guidance to file the judged verdict); mechanical negatives (diverge/cannot_verify) still auto-file. Verifier prompt updated to state the filing semantics; single-verdict invariant restored. Gated by canary FAULT O. Found independently by 5 of 10 verifiers. | **fixed** in repo + template |
| TL-5 | Low | Verdict/claim records carried `session: "s-unknown"` — the CLI had no session identity source in this environment, weakening the provenance envelope. Fixed: `TRUTH_SESSION` honored verbatim; otherwise falls back to `s-<ppid>-<yyyymmdd>` (the invoking shell/harness PID is stable for a sitting — forensic grouping, not identity). Gated by canary FAULT Q (fallback must not be s-unknown; override honored). | **fixed** in repo + template |
| TL-6 | High | **Canary FAULT A false-MISSED on macOS**: `sed -i '1s/...'` is GNU-only; BSD sed errors, the fault is never injected, the gate correctly passes the unmutated file, and the canary reports MISSED. The gate itself is sound (manually replayed: INV-A blocks a real mutation). Deeper lesson: **a canary whose fault injection can fail silently lies in both directions** — false alarm here, but a seeding failure can also masquerade as CAUGHT. Fixed: portable `sed -i.bak` + an explicit assert that the mutation landed before judging the gate. | **fixed** in repo + template |
| TL-7 | Medium | `install-hooks.sh` writes to `.git/hooks` without checking `core.hooksPath`; on husky/lefthook repos it installs dead files and prints a false "hooks installed" success. Fixed: refuses with loud guidance when hooksPath is set. Note: the copier task wraps it in `\|\| echo 'no .git yet'`, so on hooksPath repos the fallback message is misleading (stderr guidance still prints) — template task could distinguish exit codes. | **fixed** in repo + template |

## Event log

| Date | Event |
|---|---|
| 2026-07-08 | Hooks wired via husky; gate live-verified; doctor 7/7 OK. TL-1, TL-2 found. |
| 2026-07-08 | Day-0 baseline filed: 10 claims (C1–C10), actor `claude-fable-dev`. 2 freeze-doc rows found already decayed. |
| 2026-07-08 | Baseline committed (1bd4558) through the live gate; post-commit scan fired, 0 stale. |
| 2026-07-08 | **Verification round 1**: 10 fresh isolated agent sessions (actors `claude-verifier-1..10`), dispatch-only context. Result: **10/10 agree → all claims `live`**. Every verifier went beyond hash recheck and closed the evidence→text interpretation gap (blind-spot sweeps for absence claims, ADR cross-reads, SQLite magic-byte check, addon manifest inspection). One letter-vs-spirit note recorded in tr-8962a692's basis (bpy host-provided, not declared). |
| 2026-07-08 | Concurrency stress (incidental): 10 parallel same-machine verifier sessions, 20 appends — 30/30 records validate, no corruption. First empirical datapoint for the `O_APPEND` assumption. |
| 2026-07-08 | **Calibration caveat on round 1 unanimity**: all verifiers judged without VERDICT RULES (dropped in transit, TL-3), i.e. without the anti-sycophancy instruction. Their independent evidence work argues the agreements are sound, but round 2 must run with verified-intact prompts. |
| 2026-07-08 | Template updated + `copier copy` re-run by Michal. N1 held: populated ledger untouched (30/30 validate). AGENTS.md skip preserved our snippet. Canary went red: FAULT A MISSED. |
| 2026-07-08 | FAULT A diagnosed as canary self-defect (TL-6), not a gate hole; gate manually re-verified (blocks real mutation, INV-A). Canary + install-hooks fixed in repo and template; **canary 22/22 green**. Residual: `install-hooks.sh` (pre-fix) left inert hooks in `.git/hooks/` — dead while husky owns hooksPath, but would silently activate if hooksPath were ever unset; left in place as a documented fallback. |
| 2026-07-08 | **TL-1 and TL-4 fixed upstream** (doctor hooksPath resolution; recheck no longer auto-files agree). Verified: canary **25/25** (two new doctor checks + FAULT O), core suite 41/41 with armed drift detector (via `uv run --with jsonschema` — homebrew pip is broken on this machine), v0.4 regressions 12/12, doctor day-0 scenario now FAILs correctly, live recheck files nothing (ledger 30→30). Mirrored to template. Remaining upstream: TL-2 (cosmetic), TL-3 (prompt integrity), TL-5 (session id). |
| 2026-07-08 | **TL-3 and TL-5 fixed upstream** (dispatch integrity envelope: rule count + sha256 terminator; session falls back to `s-<ppid>-<date>`). Verified: canary **28/28** (FAULT P dispatch self-description, FAULT Q session identity ×2), core 42/42 armed, regressions 12/12; live dispatch on tr-ec698a92 states 4 rules = actual, terminator hash = `shasum` of prompt file; live session fallback `s-65479-20260708`, override honored. Mirrored to template. Remaining upstream: TL-2 only (cosmetic BSD head wart). Verification round 2 should relaunch verifiers with the integrity-checked dispatch. |
| 2026-07-08 | **Verification round 2**: 10 fresh isolated sessions (`claude-verifier-r2-1..10`), courier prompt identical to round 1 (clean A/B on the envelope). Result: **10/10 agree, all claims `live`; round 1's unanimity confirmed with VERDICT RULES in hand.** TL-3 fix field-validated 10/10: the proxy dropped rule 3 again in every session; every verifier detected it via the rule-count check, recovered by reading `prompts/truth-verifier.md` from disk, and hash-confirmed it against the terminator before judging. Key design datapoint: the terminator *survived* compression in all sessions — the rule-count header, not the terminator, did the catching; neither alone suffices. TL-4 validated in the wild: exactly 1 verdict per verifier (ledger 30→40, zero auto-agrees). TL-5 visible: all 10 verdicts carry `s-r2-N` sessions. Verifiers again out-worked the evidence commands (recursive finds into subpackages, `.venv`/`uv.lock` sweeps, ADR deliverable audits). |
| 2026-07-08 | **TL-8 (environment, new)**: the sqz proxy *deduplicates hash strings out of command output* — `shasum -a 256 prompts/truth-verifier.md`, the exact command the envelope prescribes, returned an opaque `§ref:…§` token in most sessions because the digest already appeared in the terminator. Verifiers recovered via scratchpad files / case-mangling. Hazard class: verify-by-hash workflows vs. deduplicating transports. Suggested upstream hardening: the envelope should prescribe a *local comparison* command that prints MATCH/MISMATCH (never echoes the digest), e.g. a `truth dispatch --check` subcommand. Not yet implemented. |
| 2026-07-08 | **First live decay event (real development).** ADR-011 old-BOM deletion (`f819da9`, kitchen-erp, 16 files) tripped the post-commit scan: 3 claims staled. Fact-level precision **1/3** — tr-6e83eb77 true positive (fact killed → diverged, queued for human retraction); tr-50764deb and tr-d7dd1870 false positives in fact terms (coarse `kitchen-erp/**` tripwires fired on unrelated edits; facts unchanged → re-verified live, anchors advanced, F2-durability now doing real work). Successor claim tr-65e723dd filed. Cost of the event: 2 re-verifications + 1 diverge ≈ 3 minutes. Also: kitchen-erp suite found at 38 pass / 15 broken (no baseline existed) — more evidence for the unaudited-population thesis; repaired to 66 pass before the deletion. |
| 2026-07-08 | **Proxy removed by Michal; transport re-verified clean.** Main session: dispatch arrives with all 4 rules (rule 3 visible in transit for the first time), raw `shasum` output uncorrupted, no banners. Fresh-subagent probe (read-only, no verdict filed): PASS/PASS/PASS — rule count 4/4 with no gaps, terminator digest = on-disk hash, zero compression artifacts. Full battery green post-removal: canary 28/28, core 42/42 armed, regressions 12/12, doctor 0/0, ledger 40/40. TL-3/TL-8 *environmental* hazard is gone; the integrity envelope stays — it is defense-in-depth against any future lossy transport and costs two lines. |
| 2026-07-08 | **First human retraction (trial's human-gate exercised).** Michal ran `TRUTH_HUMAN=1 scripts/truth verdict tr-6e83eb77 retracted --basis "superseded by tr-65e723dd after f819da9"` himself from the interactive session. Notable: the command first arrived as a chat message and the agent **refused to execute it on the human's behalf** — the `TRUTH_HUMAN` guarantee is "a human ran this", not "an agent ran it citing human approval"; Michal re-ran it via shell passthrough. Post-state: queue empty (no divergence, no stale P0/P1), doctor 0/0, tr-6e83eb77 `retracted`, successor tr-65e723dd `unverified` awaiting an independent verification round. Full decay lifecycle now demonstrated end-to-end: claim → tripwire → diverge → successor → human tombstone. |
| 2026-07-08 | **Mini verification round 3 — FIRST GENUINE DIVERGE (author overstatement caught).** Single fresh isolated verifier (`s-mini-r3-1`) on successor tr-65e723dd. Verdict: **diverge** — the claim text says "no calculate_cost or use_new_bom ANYWHERE" but the evidence grep (all of `kitchen-erp/`, returncode 0) matches 3 lines in the pinning tests; verifier also flagged that `evidence_paths` declares production-only scope while the command sweeps `tests/` — text, command, and scope disagree, and a matching hash "locks in the contradiction." Author (this session) confirms: real wording defect, filed at session 2's close. Anti-sycophancy validated against the trial operator's own claim — 21 prior verdicts were unanimous agrees; the first false claim offered was caught by n=1. Verifier also noted the substance (production clean) is true and would have agreed with a scoped text. Replaced by tr-b270996f (text/command/paths all scoped to `kitchen_erp/`). |
| 2026-07-08 | **Template update: Beads tracker integration (`04c376e`→`4c74f36` via `copier update --vcs-ref HEAD`).** Brings `scripts/truth-bd-adapter.sh` (normalizes any bd JSON shape to the `[{id,title}]` join contract, fails loudly on unrecognizable ids) + 2 canary checks driving the adapter with fake trackers. Battery green post-update: **canary 30/30, core 42/42, doctor 0/0**; `ready` degradation verified live (bd absent → guidance, not traceback; `--stdin` join works). Two operational notes: (1) plain `copier copy gh:` would have DOWNGRADED to tag v0.4.0 (`0bfe83f`, 9 commits back) — copier defaults to latest tag; `--vcs-ref HEAD` required until a v0.4.1 tag exists. (2) Template packaging wart: template/AGENTS.md's new pointer references `docs/beads-integration-guide.md`, which lives in the template repo, not in `template/` — dangles in scaffolded projects. TL-7 install-hooks guard fired correctly during the update (refused hooksPath clobber, printed husky wiring). `bd` itself NOT installed here — adapter verified by canary fakes; ledger runs standalone. |
| 2026-07-08 | **ADR-002 accepted and implemented: v0.5 native work kernel; A/B vs Beads shows parity.** Upstream `fca6f60` (tag v0.5.0): `issue`/`issue_event` record kinds in the same confluent fold (issue payload last-write-wins; `cancelled` terminal + TRUTH_HUMAN-gated; `closed` reopenable), verbs issue/start/done/issues, premise-at-birth warning, `done --claim` atomic both-or-neither, ready precedence stdin > TRUTH_TRACKER_CMD > native > bd. Gates grown per ADR: core **42→55** tests, canary **30→42** (FAULT R1–R8: premise warning, unknown-dep cycle defense, native HELD/pass, dep gate both directions, seam==kernel equality, human-gated cancel + terminality, claim-at-death atomicity both ways, INV-A on issue records). kuchnie updated (`aa3a63d`), full battery green. **A/B run live:** native twins `wk-d5df7e30` (Material mirror ← tr-d7dd1870) and `wk-03434168` (krono ← tr-88dc0d9a, dep on mirror) filed beside Beads kuchnie-8gc/9vz; native `truth ready` and Beads-adapter `truth ready` both surface exactly the Material-mirror item and hold krono behind its dep; `issues --ready-json \| ready --stdin` == native (seam==kernel, live). A/B verdict so far: **parity at zero external dependency**; decision point on retiring Beads comes after the Material-mirror work exercises claim-at-death for real. Ops note: `next.md` (untracked user file) vanished from the working tree during the bd-init/copier sequence; recovered byte-identical from a dangling stash commit's untracked-files tree (`a547686^3`) — stop using `git stash -u` around bd. |
| 2026-07-08 | **Beads live: findings fixed upstream, tracker wired, first premised issues.** (1) Tagged `v0.4.1` (upstream `2725072`) — plain `copier copy/update` no longer downgrades to v0.4.0. (2) Guide shipped into `template/docs/` — AGENTS.md pointer no longer dangles; kuchnie updated to v0.4.1 via default tag resolution (proves fix 1). (3) `bd` 1.1.0 installed (`npm i -g @beads/bd`), `bd init` run — NOTE: init is invasive (auto-committed 27 files incl. its own `CLAUDE.md`, `.claude/settings.json` SessionStart hook `bd prime`, Codex hooks, blanket `*.db` gitignore — audited: no tracked .db affected). Its boilerplate tells agents `bd ready`; precedence notes added to AGENTS.md + CLAUDE.md: **work-finding starts at `scripts/truth ready`**. First real issues filed: `kuchnie-8gc` (Material mirror, premised on tr-d7dd1870 via tr-8656ffb9) and `kuchnie-9vz` (krono catalog routing, premised on tr-88dc0d9a, bd-dependent on 8gc). Live join verified: `truth ready` via adapter shows 8gc only (9vz dependency-blocked by Beads). The HELD path will fire for real when mirror work stales tr-d7dd1870. Beads issue data is Dolt-local (not in git) — sync via `bd dolt push` or JSONL export. |
| 2026-07-08 | **Second live decay event — ADR-011 phase 2 (`c9fd86c`).** BOMGenerator panel geometry now from `kuchnie_core.decompose()` via new `domain_adapter.py`; suite 66→76; wall-cabinet reference price re-hand-computed from construction ($155.80 formula estimate → $141.05 real panels); ghost-front-cost bug fixed on the way. Post-commit scan staled exactly 1 claim: tr-50764deb (zero kuchnie_core imports) — true positive, **precision 1/1** (tr-d7dd1870's Material-scoped tripwire correctly did NOT fire this time). Diverged + successor tr-b485d74c filed. Queued for Michal: retract tr-65e723dd and tr-50764deb; verify tr-b485d74c/tr-b270996f next round. |
| 2026-07-08 | **Mini verification round 4 — SECOND GENUINE DIVERGE (same failure class, same author).** Two fresh isolated verifiers on the phase-2 successors. tr-b485d74c: **agree** — verifier re-ran the grep (2 matches, the 2 named files) and closed the imports-vs-conclusion gap by reading `bom_generator.generate()` (quantities really come from `decompose()`; recipe formulas only for unmapped kinds). tr-b270996f: **diverge** — recheck hash matched (production package genuinely clean) but the text's repo-wide clause "the only occurrences in the repo are negative pinning assertions in tests" is false: `attic/kitchen-erp-validate_migration.py:114,215` carry real `calculate_cost()` calls (archived in the same f819a9 commit). Pattern confirmed: both of this author's diverges (rounds 3 and 4) are the same defect — claim text asserting beyond the evidence command's scope on absence claims. Lesson for claim discipline: **never write a repo-wide clause backed by a package-scoped grep.** Successor tr-3813b9b5 filed (text scoped to the package, attic/tests named as by-design survivors), independently verified **agree** same round (verifier swept without the `--include` filter to prove the flag hid nothing). Score: 24 agree / 2 diverge / 0 cannot_verify across 26 dispatches; both diverges true positives against the operator. Queued for Michal: retract tr-b270996f. |
