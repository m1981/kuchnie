# arch-smells accepted findings — deliberate deferrals, each with a work item:
# god-class + add_module -> wk-b669f4f5 (kuchnie-hij, ERP state split)
# Regenerate: bash scripts/session-gates.d/60-arch-smells.sh --write-baseline
#
# Pruned by hand 2026-08-02, NOT with --write-baseline: regenerating would
# have silently accepted four unrelated NEW findings alongside the fixes.
#   - the three drawer-box param-bloat entries are GONE, genuinely fixed by
#     kuchnie-27b + kuchnie-b30 (DrawerBoxSpec): decompose_drawer_box 10 -> 2
#     and 11 -> 1, make_runner_accessory 8 -> 2.
#   - KitchenState 32 -> 30, an improvement from kuchnie-26s moving the
#     _ensure_*_schema migrations out of the Reflex state class.
#   - AdminState 36 -> 38. This one GREW, from the two setters kuchnie-h45
#     step 1 added for the new structure/thickness_mm fields. Recorded here
#     rather than left as a permanent NEW warning (a gate that always warns
#     gets ignored), but flagged in kuchnie-8ky so the growth is not silent.
#     Regenerate this line downward as part of the split, not before it.
god-class       kitchen_erp/admin_state.py: AdminState has 38 methods (split responsibilities)
god-class       kitchen_erp/state.py: KitchenState has 30 methods (split responsibilities)
param-bloat     kitchen_erp/state.py: add_module() takes 12 parameters (parameter-object candidate)
