# Copy to .env.local for local overrides (gitignored).
# VITE_ prefix is required for variables to be exposed to the browser bundle.

# Backend base URL — change this when deploying behind a reverse proxy.
VITE_API_BASE=http://127.0.0.1:8000
